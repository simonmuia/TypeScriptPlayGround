const superHeros: string[] = []; //how to define arrays
// superHeroes.push("spiderman");

const heroPower: Array<number> =[];

type User = {
    name:string
    isActive: boolean

}

const MLModels: number[][] = [
    [24,23,23],
    [23,23,42]
]

const allUsers: User[] = []

superHeros.push("superman")

allUsers.push({name:"", isActive: true})